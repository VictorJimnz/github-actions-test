name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo s string
        run: echho "Hello World!!"
      - name: multiline script
        run: |
          java -version